"""Earnings-season watch layer: calendar nudges and filing-landed polling.

Scope discipline (journal/JOURNAL.md, docs/evaluation_protocol.md Track 1):
this package automates the SCAFFOLDING around the analyst — reminding before a
print, and running the engine the moment a filing lands — and never the
analyst's own steps. It cannot write a thesis and it must never cause a report
to exist before one does. See `poller.thesis_state` for the enforcement point.
"""
